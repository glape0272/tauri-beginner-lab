# TS-06: フォーム入力をオブジェクトにまとめる

## 対象ファイル

- `index.html`
- `src/main.ts`

## 要件

- タイトル、本文、学習時間の入力欄を作る
- 入力値を1つのオブジェクトにまとめる
- Rust commandへ渡せる形にする

## 制約

- 数値入力は`Number()`で変換
- 入力取得と送信処理を分ける

## 完了条件

- TypeScript側のオブジェクトとRust側の引数の対応を説明できる
